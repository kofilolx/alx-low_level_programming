#include "main.h"
#define int result

/**
 * mul - Multiplies two integers
 * @a: Int representing an integer
 * @b: Int representing an integer
 * Return: Always 0.
 */
int mul(int a, int b)
{
	result = (a * b);
	return (result);
}
